# AI投资想法体检器 Agent Rules

## 项目身份

本项目当前定位为：AI投资想法体检器。

一句话定位：看到热点想买？先让 AI 体检一下。

核心用户路径：输入投资想法 -> 识别想法类型与情绪 -> 虚拟样本回放 -> 风险反例 -> 小白提醒 -> 生成分享内容。

## 当前阶段

当前阶段是 MVP 收敛与流量验证期，不是功能扩张期。

本阶段目标：

1. 统一产品定位与工程入口。
2. 跑通体检卡主链路。
3. 强化分享与内容生成能力。
4. 建立清晰合规边界与测试门禁。
5. 保留旧策略链路为降级能力，不再作为首页主入口。

## 当前主维护路径

- 后端主入口：`apps/api/alpha_sim/main.py`
- 前端主入口：`app/static/index.html`、`app/static/app.js`、`app/static/styles.css`
- 业务逻辑：`app/services`
- 测试：`tests`
- 当前执行文档：`docs/current`
- 历史产品材料：`docs/product`
- 技术材料：`docs/technical`

## 当前暂停范围

以下内容本阶段不主动开发：

- `apps/web` Next.js 完整前端
- 商业化支付
- 多市场扩展
- 真实券商接入
- 真实交易
- 自动下单
- 跟单按钮
- 复杂 RAG 知识库
- 完整市场资讯系统
- 高级策略模板商城

这些内容可以保留代码或文档，但不得影响 P0 主链路。

## P0 功能定义

P0 只包含：

1. 首页说明产品定位和合规边界。
2. 用户可以输入投资想法。
3. 系统返回想法体检卡。
4. 用户可以查看今日热点体检榜。
5. 用户可以生成可分享内容素材。

旧链路 `compile -> backtest -> simulation -> leaderboard` 保留为降级能力，不作为首页主卖点。

## 首页信息架构

首页只允许出现：

1. Hero：产品定位 + 合规边界
2. 主 CTA：生成想法体检卡
3. 想法输入区
4. 想法体检卡
5. 今日热点体检榜
6. 内容生成区
7. 合规声明

首页禁止堆叠：

- 数据源详情
- 知识库详情
- 风险卡详情
- 市场解释详情
- Ask Alpha 完整问答
- 商业化说明
- 多市场扩展说明

这些内容只能作为二级入口或后续 P1/P2。

## 当前 P0 API

- `GET /api/content/home`
- `POST /api/ideas/diagnose`
- `GET /api/ideas/trending`
- `POST /api/content/share-card`

## 合规边界

所有用户可见文案必须遵守：

- 仅用于投资想法复盘、虚拟样本回放和内容生成
- 不接真实资金
- 不接券商
- 不自动下单
- 不提供跟单
- 不构成投资建议
- 不承诺收益
- 不输出明确买卖指令
- 不输出目标价

- 用户输入保留原文；含禁词时返回 `warning`，不替换原文
- 系统输出必须包含 `replay_type: demo_virtual_sample`，明确虚拟样本回放

禁用词示例：

- 赚钱、稳赚、必胜、买入、卖出、目标价、跟着买、牛股、荐股、带单、翻倍、无风险、保收益

## 工程规则

1. 不要一次性重写整个项目。
2. 不要同时修改两套前端。
3. 不要新增生产依赖，除非明确说明原因。
4. 不要复制 `参考内容/` 中第三方项目的大段代码。
5. 不要把 P1/P2 功能塞回首页。
6. 不要制造新的启动入口混乱。
7. 修改 API 返回结构时必须同步更新前端和测试。
8. 修改前端 DOM id 时必须同步更新 JS 和测试。
9. 如果无法完成测试，必须说明具体原因，不允许只写“未测试”。

## 默认命令

启动服务：

```powershell
py -3 -m uvicorn apps.api.alpha_sim.main:app --reload --port 8000
```

运行测试：

```powershell
py -3 -m pytest tests/ -q
```

健康检查：

```text
http://127.0.0.1:8000/api/v1/health
```

## 每次任务输出格式

完成任务后必须输出：

### 修改摘要

- 修改了什么
- 为什么修改

### 文件列表

- 新增文件
- 修改文件
- 删除文件，如有

### 验收结果

- 实际运行的命令
- 测试结果
- 未运行测试的原因

### 风险与遗留

- 仍然存在的问题
- 下一步建议

## Definition of Done

一次任务只有同时满足以下条件，才算完成：

- 任务开始前已阅读 `docs/current/P0_ACCEPTANCE.md`，结束前逐条验收。
- P0 主链路没有被破坏。
- 首页可以加载。
- 核心 API 可以返回。
- 测试命令已运行或明确说明失败原因。
- 修改范围没有越界。
- 输出了验收结果。
